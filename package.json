{
  "name": "book-review-platform",
  "version": "1.0.0",
  "description": "A full-stack book review platform with React frontend and Node.js backend",
  "scripts": {
    "install-all": "cd frontend && npm install && cd ../backend && npm install",
    "dev": "concurrently \"cd frontend && npm run dev\" \"cd backend && npm run dev\""
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  },
  "private": true,
  "workspaces": [
    "frontend",
    "backend"
  ]
}